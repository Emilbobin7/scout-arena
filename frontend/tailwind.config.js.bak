/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {
            colors: {
                primary: "#1a56db", // Blue-600
                secondary: "#0f172a", // Slate-900 (Black-ish)
                accent: "#22c55e", // Green-500 (Neon-ish)
            },
            fontFamily: {
                sans: ['Inter', 'sans-serif'],
            }
        },
    },
    plugins: [],
}
